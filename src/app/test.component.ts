import { Component, Input } from '@angular/core';

@Component({
  selector: 'app-test',
  styles: [`p { font-family: Lato; }`],
  templateUrl: 'test.component.html'
})
export class TestComponent  {
  //create one input variable name animals that is array of string 
}
